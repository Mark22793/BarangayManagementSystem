@model BarangayCMS.Areas.Staff.ViewModels.ComplaintViewModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Remove Complaint Log</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <div class="container py-5" style="max-width: 550px;">
        <div class="card border-0 shadow-sm p-4 rounded-3 border-top border-danger border-4">
            <div class="text-center text-danger mb-3">
                <i class="bi bi-shield-fill-x fs-1"></i>
                <h5 class="fw-bold text-dark mt-2 mb-1">Nais mo bang burahin ang kasong ito?</h5>
                <p class="text-muted small">Ang pagbura sa complaint log ay permanenteng aalisin ito sa kasalukuyang desk view list.</p>
            </div>

            <div class="bg-light p-3 rounded-2 mb-4 border small text-start">
                <div class="mb-1"><strong>Case Subject:</strong> <span class="text-secondary">@Model.Subject</span></div>
                <div><strong>Complainant:</strong> <span class="text-secondary">@Model.ResidentName</span></div>
            </div>

            <form asp-action="Delete" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="ComplaintId" />
                <div class="d-flex justify-content-center gap-2">
                    <a asp-action="Index" class="btn btn-sm btn-light border px-4">Cancel</a>
                    <button type="submit" class="btn btn-sm btn-danger px-4 fw-semibold">Confirm Delete</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>